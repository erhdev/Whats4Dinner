const assert = require('chai').assert;
const app = require('../CHANGE');

describe('Restaurants', function () {
    it('home page should show all restaurants in database', function (){
        assert.equal()
    });
});